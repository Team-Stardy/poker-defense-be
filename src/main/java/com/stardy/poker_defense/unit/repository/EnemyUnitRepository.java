package com.stardy.poker_defense.unit.repository;

import com.stardy.poker_defense.unit.entity.EnemyUnit;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EnemyUnitRepository extends JpaRepository<EnemyUnit, Long> {
}
